replicaCount: 1

image:
  repository: proxy.link-vaults.com/proxy/linkvault-lynx/bennyyang11/linkvault
  tag: "v1.0.0"
  pullPolicy: IfNotPresent

initImage:
  repository: proxy.link-vaults.com/proxy/linkvault-lynx/busybox
  tag: "1.36"

nameOverride: ""
fullnameOverride: ""

service:
  type: NodePort
  port: 8080
  nodePort: 30080

ingress:
  enabled: false
  host: ""
  className: nginx
  annotations: {}
  tls:
    enabled: false
    certManager:
      enabled: false
      issuerName: "letsencrypt-prod"
      issuerKind: "ClusterIssuer"
    secretName: ""
    selfSigned: false

config:
  pollIntervalSeconds: "10"
  reportIntervalSeconds: "60"
  updateCheckIntervalSeconds: "300"

# --- Embedded PostgreSQL (Bitnami subchart) ---
postgresql:
  enabled: true
  image:
    registry: proxy.link-vaults.com
    repository: proxy/linkvault-lynx/bitnami/postgresql
  auth:
    postgresPassword: "linkvault-default-pw"
    database: "linkvault"
  primary:
    persistence:
      size: 1Gi

# BYO PostgreSQL
externalPostgresql:
  host: ""
  port: "5432"
  user: "postgres"
  password: ""
  database: "linkvault"
  existingSecret: ""

# --- Embedded Redis (Bitnami subchart) ---
redis:
  enabled: true
  architecture: standalone
  image:
    registry: proxy.link-vaults.com
    repository: proxy/linkvault-lynx/bitnami/redis
  auth:
    enabled: false
  master:
    persistence:
      enabled: false

# BYO Redis
externalRedis:
  host: ""
  port: "6379"
  password: ""
  existingSecret: ""

# --- Global settings ---
linkvault-sdk:
  integration:
    licenseID: ""

global:
  security:
    allowInsecureImages: true
  imagePullSecrets:
    - name: enterprise-pull-secret
  replicated:
    licenseID: ""
